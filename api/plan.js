export default async function handler(req,res){try{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {topic}=req.body||{};const key=process.env.GEMINI_API_KEY;if(!key)return res.status(500).json({error:'GEMINI_API_KEY belum dipasang di Vercel.'});const prompt=`Buat rencana YouTube Short 60-64 detik tentang "${topic||'AI terbaru'}". Voice-over bahasa Indonesia. Tepat 8 scene, masing-masing 8 detik. Kembalikan JSON valid saja: {"script":"120-150 kata","scenes":[{"prompt":"English self-contained cinematic Veo prompt"}]}. Setiap prompt wajib portrait 9:16, konsisten karakter/setting, tanpa teks/logo/watermark, dan minta natural audio/dialogue sesuai adegan.`;const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key='+key,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseMimeType:'application/json'}})});const j=await r.json();if(!r.ok)throw Error(j.error?.message||'Gemini gagal');return res.status(200).json(JSON.parse(j.candidates[0].content.parts[0].text));}catch(e){return res.status(500).json({error:e.message})}}