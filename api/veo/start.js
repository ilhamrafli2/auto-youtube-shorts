export default async function handler(req,res){try{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {prompt}=req.body||{},key=process.env.GEMINI_API_KEY;if(!key)return res.status(500).json({error:'GEMINI_API_KEY belum dipasang di Vercel.'});const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/veo-3.1-fast-generate-preview:predictLongRunning',{method:'POST',headers:{'x-goog-api-key':key,'content-type':'application/json'},body:JSON.stringify({instances:[{prompt}],parameters:{aspectRatio:'9:16',durationSeconds:'8',resolution:'720p'}})});const j=await r.json();if(!r.ok)throw Error(j.error?.message||'Veo gagal start');return res.status(200).json({name:j.name});}catch(e){return res.status(500).json({error:e.message})}}